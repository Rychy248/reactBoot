import { WhiteContainer } from "../../Containers";

function About({ id }) {
  return(
    <WhiteContainer specialTitle="Project structure - about Component" idTitle={id}>
      <h2>About Component</h2>
    </WhiteContainer>
  );
};

export default About;