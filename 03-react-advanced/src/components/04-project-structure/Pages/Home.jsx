import { BlackContainer } from "../../Containers";

function Home({ id }) {
  return(
    <BlackContainer specialTitle="Home Component" idTitle={id}>
      <h2>Home Component</h2>
    </BlackContainer>
  );
};

export default Home;