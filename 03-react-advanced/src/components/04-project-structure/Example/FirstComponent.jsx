import { WhiteContainer } from "../../Containers";

function FirstComponent({ id }) {
  return(
    <WhiteContainer specialTitle="Project structure - First Component" idTitle={id}>
      <h2> First Component</h2>
    </WhiteContainer>
  );
};

export default FirstComponent;