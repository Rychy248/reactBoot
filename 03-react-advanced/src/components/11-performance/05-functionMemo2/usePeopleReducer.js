/**USE REDUCE, BUUUUTT. CUSTOMIZED HOOK */

import { useState, useReducer } from "react";

import { data } from "../../data";
// {id: 2, name: 'peter'}

const defaultValue = {
  people:data
};


function reducer(state, action) {
  
};

// [people, dispatchPeople]
export default usePeopleReducer = () => useReducer(reducer, defaultValue); ;